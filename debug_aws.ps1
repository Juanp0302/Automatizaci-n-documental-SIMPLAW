param(
    [string]$path_to_key = "..\simplaw.pem",
    [string]$aws_host = "ec2-user@your-ec2-ip.compute.amazonaws.com"
)

# This script is a placeholder since we don't have the user's secret AWS key locally.
# We will instead create a debug workflow in GitHub Actions to dump the logs for us to read from out here.
